-module(dbus_client_SUITE).

-include_lib("eunit/include/eunit.hrl").
-include_lib("common_test/include/ct.hrl").

%% A placeholder suite. Common Test errors on a suite with no `all/0', so it
%% declares an empty list until it has cases of its own.
-export([all/0]).

all() ->
    [].
